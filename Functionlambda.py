# Lambda Functions:
# Lambda functions, also known as anonymous functions, are small, inline functions that can have 
# any number of arguments but can only have one expression. They are defined using the lambda keyword.

double = lambda x: x * 2
print(double(3))


